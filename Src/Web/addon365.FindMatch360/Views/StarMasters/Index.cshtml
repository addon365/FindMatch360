@model IEnumerable<addon365.FindMatch360.Models.Masters.StarMaster>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StarName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StarName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StarMasterId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.StarMasterId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.StarMasterId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
